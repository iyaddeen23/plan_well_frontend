/** @type {import('next').NextConfig} */
const nextConfig = {
  // NEXT_PUBLIC_API_URL must be set as an environment variable in your
  // Vercel project settings (Settings → Environment Variables).
  // For local dev it is read from .env.local.
};

export default nextConfig;
